# EXAMPLE ONLY — copy to worker.env (gitignored).
SWARM_API_AUTH_TOKEN=replace-with-seed-or-operator-token
SWARM_SERVER_URL=http://127.0.0.1:18766
SWARM_PUBLIC_HOSTNAME=worker.example.local
SWARM_PROCESS_ROLE=worker
SWARM_ALLOW_PAID=false
SWARM_ENABLE_MACOS_ADAPTER=false
