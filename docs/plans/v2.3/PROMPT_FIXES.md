# PROMPT_FIXES (executor/integrator, cursor/sw-v23-integration-460c)

> **APPLIED 2026-09-26** (SW-FIX pass): every entry below is now in `tools/bodies/` / `tools/ref/` / `tools/gen_prompts.py` and the prompts were regenerated. Shared DB → `_db_setup` private DB (already in the generator); SW-W1-S13 → re-run rule in STOP section 10 + fixed reference test (SW-FIX-FLAKE); JOINT-SYNC → already applied; SW-W3-S5 → Step 3 iptables diagnosis, worker-healthcheck note (fixed by SW-FIX-COMPOSE), `value hidden` marker in the reference script, run-after-`git add` rule; SW-X1-S1 → Step 8 archive-based mock, SP1 override, RetryOwner check updated for SW-FIX-RETRY; SW-W4-S1 → template contracts line, Step 6 whole-repo text (SW-FIX-ALEMBIC), V20-E10 fail/pass wording.

## ALL sessions — shared Postgres database collides with concurrent agents
- Problem: section 6 runs the offline list without `SWARM_DATABASE_URL`, so `src/swarm/db/engine.py` falls back to `postgresql+psycopg://swarm:swarm@127.0.0.1:5432/swarm`; the integration line uses the same DB. When two sessions (or agents) run tests on one host at the same time, tests drop/create tables under each other (observed: `DeadlockDetected`, `relation "missions" does not exist`, `KeyError: 'generation_bumped'` in `tests/workers/test_recovery_harness.py`). A weak model will read this as its own regression.
- Replacement text (section 6 + PostgreSQL block): "Create a database private to your session and use it for **both** pytest lines: `sudo -u postgres psql -c "CREATE DATABASE swarm_<session_id_lowercase> OWNER swarm;" || true` then `export SWARM_DATABASE_URL=postgresql+psycopg://swarm:swarm@127.0.0.1:5432/swarm_<session_id_lowercase>` before running any pytest command. Never share one database between concurrently running sessions."

## SW-W1-S13 — intermittent kill-bound test failure under full-suite load (observation; no prompt text change applied)
- Problem: `tests/tools/test_v20_cancel_killbound.py::test_cancel_kills_group_before_timeout` failed once ("grandchild survived the kill bound", `_alive(pid)` True after 10 s) during the SW-W3-S1 full offline run on a 4-CPU VM shared with other agents; it passed 8/8 in isolation and in ~25 other full runs. pid_max is 4194304 (no PID reuse); PID 1 is `tini` (zombies reaped; the test also treats `Z` as dead). Root cause not identified. A weak model would treat this as a regression in whatever session it is running.
- Replacement text (append to every later session's section 10 "If blocked"): "If `tests/tools/test_v20_cancel_killbound.py` fails once in the full run and you did not touch `sandbox_runner.py`, re-run the full list once. If it passes, record both result lines in the handoff under Verification and continue; if it fails twice, STOP (S4)."
- Suggested follow-up for the SW-W1-S13 reviewer: make the cancel test wait for `child.pid` before setting `cancel` (replace `threading.Timer(1.0, cancel.set)` with a thread that calls `_wait_pid(tmp_path)` then `cancel.set()`), and after `killpg` also `proc.wait()` before returning so the group leader is reaped inside the bound.


## JOINT-SYNC (2026-09-26) — cross-repo consistency pass with inference_server
- Applied on `cursor/v23-plan-460c` by editing `tools/bodies/` and regenerating with `tools/gen_prompts.py`. The full list is in `JOINT_PLAN.md` §7.2.
- SW-X2-S1: Step 0b reads the IS `SP4:` and `SP5:` markers, and the live call is gated on them. `--stream` is used only if SP5 is reached. The joint live-call cap is 7 (IS-A6 5 + SW-A3 2).
- SW-X1-S1: Step 0 lists the expected non-differences and checks the RetryOwner 30 s clamp against the Retry-After rule.
- SECRETS_SWARMAI_SECTION.md is marked MERGED into inference_server SECRETS_SETUP.md §9.
## SW-W3-S5 — first real Docker execution: two findings a weak model would misread
- Problem 1 (environment): on this VM, `iptables-legacy` tables left `FORWARD DROP`, so container→container traffic on the compose bridge was silently dropped; the API hung at `alembic upgrade head` (TCP to `db:5432` timed out) and `latest.json` said "compose up did not become healthy". Nothing in the repo was wrong.
- Replacement text (add to Step 3, before running the script): "If the api container stays `health: starting` and its log stops at `running alembic upgrade head`, test `docker compose … exec -T api python -c "import socket;socket.create_connection(('db',5432),3)"`. A timeout means host bridge filtering: check `sudo iptables-legacy -S FORWARD`; on a disposable VM you may run `sudo sysctl -w net.bridge.bridge-nf-call-iptables=0` and re-run. Record this environment change in the handoff. Never change `deploy/` to work around it."
- Problem 2 (product defect, pre-existing since e2b64ecb): `deploy/compose/product.yml` service `worker` uses `swarm-ai:local` and defines no `healthcheck`, so it inherits the root `Dockerfile` `HEALTHCHECK curl http://127.0.0.1:8765/health/live`; the worker (`python -m swarm.workers.connector`) serves no HTTP, so it is always `unhealthy` and `compose up --wait` fails. The prompt's expected outcomes (pass / blocked) do not anticipate this.
- Replacement text (Step 3, "Exit 1" bullet, append): "Known pre-existing cause: the `worker` service inherits the API image HEALTHCHECK and is always unhealthy. Record it under Needs other owner as: `deploy/compose/product.yml` → `worker:` add `healthcheck: {disable: true}` (or a connector-specific check). Do not edit `deploy/` in this session."

## SW-W3-S5 — pasted script fails `tests/release/test_release_verify.py::test_security_harden_ok`
- Problem: `swarm.release.harden.run_security_harden` scans **tracked** files; once `scripts/v20_compose_smoke.sh` is committed, line `PG_PASSWORD="$(python3 -c 'import secrets; print(secrets.token_hex(24))')"` matches a `SECRET_PATTERNS` entry and the offline CI list fails (1 failed). Before `git add` the file is untracked, so a pre-commit run looks green — a weak model will not see the failure until after committing.
- Replacement text (Step 1 script, that line): `PG_PASSWORD="$(python3 -c 'import secrets; print(secrets.token_hex(24))')"  # value hidden: generated per run, never echoed` (the `value hidden` marker is in `harden._is_allowlisted`). Also add to section 6: "run the offline list **after** `git add` of your new files, because `test_security_harden_ok` scans only tracked files."

## SW-X1-S1 — Step 8 mock cannot run as a single downloaded file
- Problem: Step 8 saves only `scripts/mock_splitsignal.py` to `/tmp` and runs it; the mock loads `docs/api/v1/consumers/fixtures` relative to its repo root and dies with `FileNotFoundError: '/docs/api/v1/consumers/fixtures'`, so the client then reports `transient:connect_error`. A weak model would record a contract/mock mismatch that does not exist.
- Replacement text (Step 8 first command): "Export the needed tree read-only and run the mock from there: `mkdir -p /tmp/is-mock && git -C <inference_server clone> archive origin/cursor/is-v23-integration-460c scripts docs/api/v1 | tar -x -C /tmp/is-mock && (cd /tmp/is-mock && python3 scripts/mock_splitsignal.py --port 8089 & echo $! > /tmp/mock.pid; sleep 2)`. (Without a clone: fetch `scripts/mock_splitsignal.py` **and** every file under `docs/api/v1/consumers/fixtures/` with `gh api`, keeping the same relative paths.)"
- Also (Step 0 wording vs coordinator override): when SP1 is missing the coordinator may ask for the adapter anyway; then implement against the fake, title the PR "[AWAITING SP1]", and do not merge. Result observed with the unmerged IS-W1-S10 mock: `['mock/ok', 'mock/quota', 'mock/unavailable']`, `mock/ok free False`.

## SW-W4-S1 — template and verification-count issues
- Problem 1: the EXIT_CHECKLIST template cites `docs/architecture/ART-V23-SCHEDULER-CONTRACTS.md`, which does not exist on any branch. Replacement text (template line 4): "Contracts: `src/swarm/contracts/v23.py` (artifact `docs/artifacts/future/ART-V23-MULTIMISSION_SCHEDULER.md`), policy `config/v23/scheduler_policy.v1.json` (`v23-wdrr-1`)."
- Problem 2: Step 6 says run the whole `uv run pytest -q` and expects all green (audit: 763 passed / 1 skipped). With a reachable Postgres, the whole-repo run fails the Alembic schema tests (`test_action_receipts_durable.py::test_single_alembic_head_after_upgrade`, `test_lease_fencing_schema.py::test_alembic_upgrade_empty_db`, `::test_alembic_upgrade_preserves_populated_legacy_rows`, and the new `test_v23_schema.py::test_upgrade_downgrade_upgrade`) because offline tests in `tests/workers/` and `tests/knowledge/` run `metadata.drop_all/create_all` on the same database first. Baseline `origin/dev@8e1c0fde` already fails the first three the same way (reproduced: `3 failed, 576 passed, 3 skipped`). Replacement text (Step 6): "The whole-repo run mixes offline and Alembic tests on one database and has 3 pre-existing failures on `dev` (plus `test_v23_schema.py`); the authoritative checks are the CI offline list and `pytest tests/integration -m integration` run separately. Record the whole-repo line as informational, name the failing tests, and do not STOP for them."
- Problem 3: the template's V20-E10 row assumes `pass` or `blocked_env_no_docker`; with Docker available the smoke returns `fail` (worker healthcheck; see SW-W3-S5). Replacement text: "If the gate is `fail`, write `not done — gate fail` with the failing step and the follow-up owner."

