# OpenRouter free-route prerequisite — offline candidate

Observed 2026-09-22 21:13 UTC. Anonymous read-only OpenRouter `GET /api/v1/models` listed `qwen/qwen3.8-27b:free` with prompt and completion prices both `0`. Anonymous `GET /api/v1/models/qwen/qwen3.8-27b:free/endpoints` listed one endpoint, `ModelRun`, with prompt and completion prices both `0` and `max_tokens` support. Anonymous `GET /api/v1/providers` identified its routing slug as `modelrun`. These observations are a candidate selection, not account eligibility, entitlement, remaining allowance, or a live request grant. Recheck before any exact live action.

The adapter now requires an explicit `OpenRouterFreeRoute` for network modes and sends only the selected `:free` model, the selected backend in `provider.only`, `allow_fallbacks: false`, `max_price.prompt/completion: 0`, `require_parameters: true`, and a positive finite `max_tokens`. It does not send `models`, plugins, service tiers, or arbitrary extra request parameters. An unconfigured live route remains disabled and raises before the network. The historical replay fixture route is retained for offline tests only.

Official API references: [provider routing](https://openrouter.ai/docs/guides/routing/provider-selection), [model details](https://openrouter.ai/docs/api/api-reference/models/get-model), [model endpoints](https://openrouter.ai/docs/api/api-reference/endpoints/list-endpoints), [provider list](https://openrouter.ai/docs/api/api-reference/providers/list-providers), [BYOK behavior](https://openrouter.ai/docs/guides/overview/auth/byok).

Still required before a live route: authenticated account tier and key scope; explicit verification that BYOK or account settings cannot add charges; current model/backend/price and remaining allowance readback; durable broker admission and call accounting; provider charge and rate-limit receipt capture; exact inference grant. A zero request price ceiling and this offline wire test do not prove a zero actual bill or a successful free-tier route. No provider request was made in this candidate.
