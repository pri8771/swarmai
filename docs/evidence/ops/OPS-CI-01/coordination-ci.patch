--- a/.github/workflows/ci.yml	2026-09-21 20:32:52
+++ b/.github/workflows/ci.yml	2026-09-21 20:32:52
@@ -2,8 +2,19 @@
 
 on:
   push:
+    paths-ignore:
+      - "docs/**"
+      - "**/*.md"
   pull_request:
+    paths-ignore:
+      - "docs/**"
+      - "**/*.md"
 
+# One run per ref; a newer push cancels the older in-flight run.
+concurrency:
+  group: ci-${{ github.ref }}
+  cancel-in-progress: true
+
 jobs:
   offline:
     runs-on: ubuntu-latest
