# V2B-002 — reviewer calibration benchmark/scorer repair and freeze candidate

Artifact: ART-V13-REVIEWER-QUALIFICATION  
Lane: B / HOST-WIN-DEV identity  
Complexity: SP3  
Intended transition: drafting -> reviewable only; ChatGPT lead freezes independently.

## Context

`ART-V13-TASK-POOL` is independently frozen at `cursor/v2-product-lane@534476393257794c4e8ebf8d65f44fd090ab28eb` as `g13-pool-freeze-v3`. That freeze is immutable for this packet. Product-task counted qualification remains separately blocked until the lead binds a real sealed-reference bundle content digest.

The EVAL-131 protocol requires reviewer-role qualification on a separately versioned review benchmark. Existing reviewer screening was weak. Benchmark/grader debugging must happen on calibration cases only; held-out reviewer results may not be collected until the benchmark/scorer/prompt/tool identities are frozen.

## Required result

Create a versioned **reviewer calibration** benchmark/scorer candidate that measures reviewer fitness rather than prose style. It must include calibration cases that exercise at least:

1. rejection of materially wrong results;
2. acceptance of materially correct results when evidence is sufficient;
3. evidence/acceptance inconsistency detection;
4. missing or contradictory evidence detection;
5. forbidden/unauthorized action detection;
6. false-success / mock-success detection;
7. stale or mismatched artifact/candidate identity detection;
8. correct treatment of truthful blocked/unsupported outcomes;
9. reviewer handling of partial-but-useful work without over-acceptance;
10. deterministic safety/format checks that fail closed.

The calibration set may contain labels/reference rationale needed to debug the scorer, but it must be clearly marked calibration and must not be reused as held-out reviewer qualification evidence. Define a future held-out interface/identity without minting known held-out answers into worker-visible files.

Freeze or version the reviewer prompt, scorer, tool protocol, size/complexity classification, exact model-config schema and evidence schema used by later reviewer qualification. Preserve the EVAL-131 one-sided 90% Wilson rule; do not tune thresholds based on observed calibration performance.

Add direct negative tests showing the scorer rejects at least wrong-result acceptance, evidence mismatch, forbidden action, stale identity and mock/known-answer success. Add a positive control for a correct evidence-bound review.

Do not modify `benchmarks/g13/pool_freeze_v3/**`, do not start W-131B product qualification, do not start reviewer held-out qualification, and do not claim any reviewer route qualified.

## Executable verification

Actually execute and record:

- reviewer calibration generator/verifier command(s);
- focused reviewer benchmark/scorer tests;
- `uv run ruff check` on relevant changed paths;
- `uv run mypy` on relevant source;
- relevant offline pytest.

If the active implementation host is not Windows, state the actual OS truthfully. Platform-specific evidence is not required for this evaluation contract, but do not claim Windows execution when it did not occur.

## Boundaries

No additional spend. No remote provider calls. No main merge, public deploy/release, force push, secret exposure, hidden-answer leakage, known-answer substitution, or artifact self-acceptance.

## Completion

Commit/push one coherent bounded slice. Report exact parent and completion SHA, changed files, benchmark/calibration counts, frozen identity/digests, exact commands/results and remaining blockers. Request lead review only. Stop before any held-out reviewer or product counted qualification.
