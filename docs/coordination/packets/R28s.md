# R28s — enforce the development sandbox boundary

Artifact ART-V17-TOOL-CONTRACT / ART-V17-PERMISSION-NEGATIVES. SP2. Depends R28b. Source basis: `f2b8d5f:src/swarm/tools/sandbox_runner.py` runs `sys.executable` with subprocess; network=False is only a parameter check and host filesystem is not isolated. Do not report this as a container/network boundary.

Surfaces: `src/swarm/tools/sandbox_runner.py`; new `sandbox/execution/Dockerfile`; focused `tests/tools/test_sandbox_isolation.py` and real-container integration tests. Keep the existing runner API/result DTO where possible. No parallel permission system or orchestration service.

Operational adapter invokes a local operator-configured container runtime from the trusted control plane. Worker container has `--network none`, non-root UID, dropped capabilities, no-new-privileges, bounded pids/memory/CPU and timeout; read-only image plus designated worktree and ephemeral tmpfs. Never mount host home, credential stores, control sockets or whole repository parents. Runtime/image digest and actual restrictions recorded. Dependency installation/networking happens in a separate controlled image-build/preparation step, never by untrusted test execution. Require pinned prebuilt image; unavailable runtime/image is an explicit environment blocker, never a fallback to host subprocess. Cross-platform support only after real tests.

Commands are typed argv, no shell. Enforce a total timeout by stopping and waiting for the specific container, then record unknown if effects could have escaped via separately authorized external paths. Sandbox operations in this profile have no network; mount paths canonicalized and symlink escapes rejected. Read-only repo/control-plane setup is outside worker authority; all worker commands still pass the gateway.

Negatives: code attempts host sentinel read/write, outbound loopback/internet socket, access to gh/env credentials, Docker socket, worktree traversal/symlink race, fork/resource exhaustion and descendant surviving timeout. The effect file/counter proves whether a process survived; configuration text alone is not evidence. Plain unit tests may use fakes, but real isolation claim requires a real container run with named skips reported.

Evidence: focused test commands/exits, redacted runtime/image identity and sentinel results, timeout teardown proof. No product secrets used. Exit: actual enforcement on the configured private development host, or code complete with explicit `EXT-LOCAL-SANDBOX` pending; no hostile multi-tenant assurance is claimed. R28d operational/live run requires the environment gate satisfied.
