# V2B-001-R5 — actual HOST-WIN-DEV G13 v3 executable verification gate

Artifact: ART-V13-TASK-POOL  
Lane: B / actual HOST-WIN-DEV only  
Complexity: SP1  
Intended transition: reviewable -> lead-freeze candidate only.

## Why this packet exists

`cursor/v2-product-lane@534476393257794c4e8ebf8d65f44fd090ab28eb` contains a semantically strong `g13-pool-freeze-v3` candidate and exact-tip GitHub Actions `35625964121` is green. However, the implementation session that produced/reported the executable checks explicitly identified its OS as Linux while the fixed topology and `V2B-001-R4` assign final executable ownership to **HOST-WIN-DEV**. The lead therefore withheld final freeze.

This packet is a bounded platform/provenance verification gate. Do **not** remint or semantically change the v3 corpus unless Windows exposes a real portability defect that cannot be fixed otherwise.

## Required execution provenance

Run on the actual HOST-WIN-DEV Windows checkout. First publish evidence that establishes the executing platform truth, including:

- Windows OS/version evidence from PowerShell;
- current repository path;
- branch name and exact HEAD SHA;
- `git status --short` before execution;
- `uv --version` and Python version.

If the executor is Linux/WSL/container rather than the actual Windows host, fail closed, publish a precise blocker, and stop. Do not relabel a Linux execution as Windows.

## Source binding

Target source is exactly:

`cursor/v2-product-lane@534476393257794c4e8ebf8d65f44fd090ab28eb`

The v3 freeze bytes and identities should remain unchanged. If the lane has advanced, verify that the target commit is an ancestor and that `benchmarks/g13/pool_freeze_v3/**` plus the v3 generator/verifier/checker source are byte-identical to the reviewed target before using the descendant.

If Windows reveals a genuine portability defect, make only the minimum repair, rerun the entire gate, and report the new SHA. Any semantic corpus/checker/prompt/scorer/reference-interface change invalidates this packet and requires a new freeze version + lead review instead of silently changing v3.

## Required verification

Actually execute on HOST-WIN-DEV and preserve command/results evidence for:

1. the v3 generator/freeze command in verification-safe mode;
2. the v3 verifier/stats command;
3. focused G13 v3 tests, including semantic-sibling and sealed-reference negatives;
4. `uv run ruff check` for relevant changed/reviewed paths;
5. `uv run mypy` for relevant v3 source;
6. relevant offline pytest excluding explicitly integration-gated tests only according to repository convention;
7. a no-diff/digest check proving the reviewed v3 freeze was not mutated by verification.

The evidence must confirm:

- 240 held-out inputs;
- all 16 required family x size cells exist;
- 15 genuine semantic archetypes per cell;
- calibration/held-out identities remain disjoint;
- worker-visible held-out data remains input-only with opaque hidden-reference handles;
- no hidden answer/reference/rubric/grader leakage;
- `counted_qualification_ready=false` and `w131b_started=false` remain true until lead-controlled sealed-reference binding;
- the frozen one-sided 90% Wilson policy is unchanged.

## Autonomous-runner rule

This is a repo-assigned bounded packet. If the autonomous daemon starts it, that self-launch event is useful evidence, but `ART-OPS-AUTONOMOUS-WORKERS` still requires an attributable **source** push from each host. A verification/evidence-only commit does not by itself satisfy the source-push requirement. Do not create a fake source edit to satisfy autonomy acceptance.

## Boundaries

No additional spend. No remote paid/fallback inference. No W-131B counted qualification. No reviewer held-out qualification. No main merge, public deploy/release, force push, secret exposure, known-answer substitution, hidden-answer leakage, or artifact self-acceptance.

## Completion

Push an evidence/report commit if the gate passes without source changes, or the minimal repair + evidence commit if Windows exposes a real defect. Report exact parent/completion SHA, actual Windows provenance, commands/results, changed files, corpus/digest identity, and remaining blockers. Request lead freeze only. Stop before counted qualification.
