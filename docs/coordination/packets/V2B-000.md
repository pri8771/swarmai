# V2B-000 — sync reviewed integration baseline into Windows product lane

- Artifact support: `ART-V20-INTEGRATED-CANDIDATE`
- Session: Cursor B / HOST-WIN-DEV / `cursor/v2-product-lane`
- Story points: SP1
- Priority: first Windows product packet

## Goal

Bring the product lane onto the current reviewed integration baseline before G13/product work.

## Required steps

1. Preserve branch-local `SESSION_INSTRUCTIONS.md` and coordination runner files.
2. Fetch `origin/cursor/v2-integration`.
3. Merge only reviewed integration tip `9ce727842446b98cfa55c28c7e70808f57f17d7b` into `cursor/v2-product-lane`.
4. Do not import unreviewed `cursor/v2-runtime-lane` V15 work.
5. Resolve conflicts conservatively; do not discard heartbeat/autonomous-worker files.
6. Run Windows baseline:
   - `uv run ruff check .`
   - `uv run mypy src`
   - `uv run pytest -q`
   - `npm --prefix apps/console run lint`
   - `npm --prefix apps/console test -- --run`
   - `npm --prefix apps/console run build`
7. Record exact pass/skip/fail truthfully; Windows-specific portability failures are evidence.
8. Commit/push exact merge/evidence to `cursor/v2-product-lane`.

## Boundaries

- no runtime V15 WIP import;
- no main merge;
- no force push;
- no paid services;
- no self-acceptance.

## Completion

Packet is reviewable when the product branch contains only the reviewed integration baseline plus its own branch-local coordination files, with exact Windows baseline results.
