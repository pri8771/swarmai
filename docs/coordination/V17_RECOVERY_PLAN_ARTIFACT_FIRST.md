# V1.7 recovery plan — closure revision

Proposed on fable/v3-planning; no canonical activation or product acceptance occurs here. Exact packet definitions/status/basis/dependencies/gates live in V17_RECOVERY_PACKET_QUEUE.json. For initial historical diagnoses consult V17_CODE_AUDIT_20260921_2030_FABLE.md and earlier Git history.

The shortest safe source path is durable effect receipts/approval/unknown-outcome repair, authenticated lease authority, one adapter registry, actual sandbox isolation, durable mission mapping and operational worker/knowledge wiring. No static provider or in-memory fallback counts as operational completion. One implementation worker and one heartbeat producer remain.

Independent foundations: OPS-CI-01, R27a, R30a, R17a, R02a. Prefer the R27 chain, taking other foundations during explicit review gates. R17b now resolves to DB mapping + leased loop before R28d; R17c resolves to authenticated transport + separate-process CLI. R28s repairs the observed host-subprocess isolation gap. R33c resolves to a narrow credentialed adapter plus real external proof, never general privileged proc.run.

R34b reports a matrix; it cannot force implementation/live/working labels true. Source wiring requires R17c/R27d/R28s. Actual real-world proof requires R33c-2. CP2, G12/G13, physical-host, Windows and elapsed gates remain independently visible. Never relabel fixtures, unknown quality or skipped tests as passes.

See master-plan section 0 for exact stage sequence, end state, campaign timing, owner inputs and the finite final Fable checklist. Read packets/<ID>.md (or the R-LATER-V17 aggregate contract) only for the chosen packet. Historical broad R00–R34 goals remain in JSON as provenance/aggregate nodes, not competing implementation instructions.
