# Codex assignment — R28b-1 effective classification and required fence fields

Date: 2026-09-22
Owner ceiling: SwarmAI V2.0; no V2.3/V3 assignment.
Worker: Codex direct isolated implementation; no Fable handoff.
Lead release basis: `docs/coordination/reviews/ART-V17-TOOL-CONTRACT-R28A-LEAD-REVIEW.md`
Accepted prerequisite source: `b204fda738221787325ce8fd0b8db58e6e29b573`
Parent native packet: `docs/coordination/packets/R28b.md`

## Bounded slice

Implement **R28b-1 only: native R28b rules 1–3** in a new isolated branch/worktree based exactly on `b204fda738221787325ce8fd0b8db58e6e29b573`.

Before editing, reproduce the under-classification/default-fence failures and write a concise causal map.

### Required behavior

1. Add the native `OperationDecl` contract and `AdapterManifest.operations`. An operation absent from the manifest must fail `operation_not_declared`.
2. Compute effective side-effect/risk classification as the higher of envelope claim and manifest declaration. Authorization/approval/durability decisions must use the effective values. Receipts record effective side-effect and risk classification.
3. Remove valid-looking defaults for `ActionEnvelope.lease_generation` and `cancellation_generation`. For effective consequential/irreversible operations, mission/task/attempt plus both generations are required; missing authority fails `fence_missing`.

Update existing adapter manifests only as required to declare their current operations accurately. Do not implement the R28b-2 provider/actor/policy/fence seams yet.

## Required regressions

At minimum:
- envelope cannot underclassify a consequential manifest operation;
- undeclared operation denied before adapter action;
- consequential/irreversible operation missing mission/task/attempt/generation authority denied;
- effective classification is persisted on the receipt;
- existing R27/R28a fail-closed outcome/recovery tests remain green.

Run focused tests, affected PostgreSQL integration checks, full offline pytest, Ruff and mypy. Preserve exact commands/results and any skipped/not-run checks.

## Hold

**R28b-2 remains held pending independent exact-SHA lead review of R28b-1.**

No Fable routing, provider/model/external action, third CP1 attempt, spend, main merge, deployment, public release, scheduler change, V2.3 or V3 work.
