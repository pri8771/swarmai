# Codex assignment — R28d-2 mission worker boundary wiring

Date: 2026-09-22
Worker: Codex direct isolated implementation.
Lead release: ART-V17-TOOL-CONTRACT-R28D1-LEAD-REVIEW.md
Expected base: a2cfb3c1d5e326a1b15d7d23cfe3c91ea2e40bf4
Native packet: docs/coordination/packets/R28d.md

Implement R28d-2 engineering wiring only from the exact base.

Required:
- add the packet's synchronous V1.7 gateway bridge and fail if invoked from an already-running event loop;
- RepoWorker requires explicit action gateway, actor context, and project id;
- route worker command and file-write effects through proc.run and fs.write_text;
- use the packet's project/mission/task/attempt/operation/sequence effect-key contract;
- MissionRuntime constructs the per-worktree registry, LocalSandboxAdapter and gateway using the packet's store/fence/policy rules;
- add WorkerResult action_receipt_ids and carry them through mission evidence serialization;
- leave worktree creation/removal as trusted control-plane setup;
- preserve caller-visible worker return behavior.

Add the packet's negative/AST tests plus repeated-write distinct-receipt and sync-inside-running-loop coverage. Run focused mission tests, affected gateway/database checks, full offline tests, disposable-PostgreSQL suite where available, Ruff and mypy. Preserve exact source identity and failures/skips.

HOLD: do not run the brokered live_local mission in this assignment. Current provider/model execution remains held. No CP1 attempt3, Fable routing, public action, spend, merge, deployment, scheduler change or V1.8+ work.

Return exact-SHA READY_FOR_LEAD_REVIEW evidence.