# Codex assignment — R28b-2 actor/policy/fence provider authority

Date: 2026-09-22
Owner ceiling: SwarmAI V2.0; V2.3/V3 are not current assignments.
Worker: Codex direct isolated implementation; no Fable handoff.
Lead release basis: `docs/coordination/reviews/ART-V17-APPROVAL-BINDING-R28B1-LEAD-REVIEW.md`
Expected base: accepted R28b-1 `2ebaa5a0cd64b2de665202c5cf9db0dd4f0a1510`
Parent native packet: `docs/coordination/packets/R28b.md`

## Bounded slice

Implement **R28b-2 only — native rules 4–6** in a new isolated branch/worktree based exactly on the expected base above.

The existing read-only diagnostic `docs/coordination/reviews/CODEX_R28B2_DIAGNOSTIC_20260922.md` is an accepted baseline observation only: forged caller actor currently reaches reservation. Preserve it; do not rewrite it as post-repair evidence.

Before editing, reproduce/map any additional stale-policy/static-fence/scope-authority failures needed by rules 4–6.

## Required provider contracts

### Rule 4 — authority seams

Add `src/swarm/tools/fences.py` with the native contracts:
- `FenceState`: lease_generation, cancellation_generation, reserved authority mapping. V1.7 must not consume future authority fields.
- `FenceProvider.current(...)` and `FenceProvider.reader(...)`.
- `StaticFenceProvider` for bounded unit/non-leased tests only.
- `LeaseFenceProvider(session_factory)` delegating durable fence reads to the DB lease module.
- `ActorContext(actor, project_id)`.
- `PolicyProvider.effective_scopes(...)` and `current_policy_version(...)`.
- `StaticPolicyProvider` for deterministic tests.

Do not implement V1.8 site epochs or V1.9 extension grants.

### Rule 5 — durable current fence

In `src/swarm/db/lease_fencing.py`, add the native read-only `read_current_fence` function. Mission cancellation generation and task-attempt lease generation remain authoritative in the lease module. Missing/mismatched mission/attempt/project must fail `lease_not_current`.

Use row locking exactly as the native packet specifies and preserve the existing committed-admission fence reader behavior.

Tools code must not query worker/lease tables directly.

### Rule 6 — authenticated actor/policy/fence binding

Refactor the gateway to take:
- explicit store;
- `FenceProvider`;
- `PolicyProvider`;
- authenticated `ActorContext` at execution.

Remove constructor authority constants/static scope authority as specified by the native packet.

Before reservation/action, enforce in native order:
1. context project matches envelope project;
2. context actor matches envelope actor;
3. envelope policy version equals current policy version;
4. declaration scopes union requested scopes is contained in policy effective scopes;
5. provider current lease/cancellation generations match envelope;
6. committed `begin_execution` receives `fences.reader(...)` so a bump between precheck and admission still fails closed.

The caller-controlled envelope must never create actor, scope, policy or fence authority by itself.

## Required adverse tests

At minimum preserve/add:
- forged actor denied before reservation/adapter action;
- wrong project context denied;
- stale policy version denied;
- scope outside policy denied;
- stale lease detected from provider rather than constructor;
- cancellation bump between provider precheck and committed begin is rejected on real PostgreSQL with zero adapter call;
- missing/mismatched durable mission/attempt/project fails closed;
- accepted R27/R28a/R28b-1 crash/replay/approval/outcome/classification behavior remains green.

Use the owner's method: reproduce -> causal map -> narrow repair -> adverse tests -> detailed exact-source notes.

Run focused tests, affected real-PostgreSQL integration checks, full offline pytest, Ruff and mypy. Preserve exact commands, results, source SHA and skipped/not-run distinctions.

## Hold

**R28c remains HELD pending independent exact-SHA lead review of R28b-2.**

No Fable routing, provider/model/external action, third CP1 attempt, spend, main merge, deployment, public release, scheduler change, V2.3 or V3 work.
