# SwarmAI code/evidence audit — current -> V1.7

Audit date: 2026-09-21
Audited implementation branch: cursor/v17-single-session
Observed head during audit: fe6acbf6097bb32250115c5cd8b6a21acf9d060a
Reviewed baseline: 9ce727842446b98cfa55c28c7e70808f57f17d7b

## Verdict

SwarmAI is NOT at V1.7.

Under the owner's required standard — live evidence at checkpoints, not only old code/contracts — the project is behind.

The current branch is primarily V1.4 repair work. V1.5/V1.6/V1.7 implementation has not yet been integrated into this branch.

## Current branch delta

Current branch was observed 12 commits ahead of reviewed baseline.

The changed source is concentrated in:
- mission brokered inference;
- mission runtime/worker materialization;
- heartbeat scripts;
- V14 live evidence.

There are no current-branch commits implementing the V1.6 provenance/retrieval contract or V1.7 unified action/browser contract.

## V1.4 audit

### Positive
v14-real-005 is real operational evidence:
- persisted mission;
- brokered local inference;
- isolated worktree;
- material diff;
- real pytest execution;
- repair round;
- no auto-apply;
- $0.

### Independent review
CHANGES REQUIRED.

See:
docs/coordination/reviews/ART-V14-REAL-E2E-V14-REAL-005-LEAD-REVIEW.md

Reason:
the proposed cost-ledger patch does not match the actual nested cost.entries schema, and the model reviewer text contains unrelated stale context.

ART-V14-REAL-E2E remains drafting.

### Other V1.4 blockers
- G12 remote overlap: 0 admitted remote routes in canonical state.
- G13 counted qualification/reviewer qualification incomplete.
- G14 live adaptive proof blocked by G12/G13.
- LIVE-142 campaign not complete; any required real wall-clock period cannot be backfilled.

## V1.5 audit

Current single-session branch does NOT contain the durable lease/result implementation that exists on the old runtime donor branch.

Reusable donor work exists on cursor/v2-runtime-lane:
- 630ab780 — durable lease fencing schema/repositories;
- 92f59faf — legacy upgrade/token lifecycle/recursive denial repair;
- 7dcefe4c — atomic claim/renew/expire;
- e764834a + 685810cc — renew/expire authority and terminal/expiry fencing;
- DBOS reuse spike/decision support.

Missing current requirements:
- durable result acceptance fencing;
- worker service/client over durable state;
- live restart/reassignment/stale-result proof;
- actual multi-host evidence where required.

The baseline current WorkerRegistry is still largely process-memory state.

## V1.6 audit

Current memory implementation is V0.4-style file-backed memory.

Critical gap:
retrieve_context() iterates store.list_all() and ranks records without a project/actor permission filter before ranking.

Therefore:
- permission-first retrieval is not implemented;
- versioned KnowledgeItem/KnowledgeLink/Tombstone model is not implemented;
- accepted_fact vs hypothesis lifecycle is not implemented;
- deletion/supersession invalidation is not implemented;
- retrieval receipts/provenance/token accounting contract is not implemented.

V1.6 is not implementation-complete.

## V1.7 audit

Current ToolGateway provides useful V0.5 foundations:
- normalized args;
- scope checks;
- payload hash;
- side-effect approval;
- lease generation check;
- process-local idempotency;
- unknown outcome receipt.

But V1.7 requires substantially more.

Current gaps:
- no full ActionEnvelope contract with project/actor/integration/destination/effect key/policy/cancellation binding;
- approvals/effect receipts are not durably persisted as the V1.7 authoritative boundary;
- _seen_ops is process-local, so restart-safe exactly-once consequential effect semantics are not proven;
- no unified adapter manifest across local/API/browser session classes;
- no browser/session recovery implementation;
- no complete negative suite for payload/destination/expiry/cancel/stale generation/unknown outcome;
- no live three-integration checkpoint.

V1.7 is not implementation-complete.

## CI audit

Reviewed baseline CI was green.

Every observed current-branch GitHub Actions run after the single-session bootstrap failed within seconds with zero job steps reported by the GitHub job API.

This does NOT prove a code-test failure; it indicates a workflow/job-start infrastructure/account/config failure that must be classified.

Until classified:
- exact-tip GitHub CI is not green;
- local test evidence must be preserved separately;
- no universal "green" claim is allowed.

## Live checkpoint status

- heartbeat/liveness: live and functioning;
- V1.4 real mission: operational execution achieved, semantic independent review failed;
- V1.5 live durable worker checkpoint: absent on current branch;
- V1.6 live knowledge checkpoint: absent;
- V1.7 live action/session checkpoint: absent.

## Required response

Stop broad version-level work.

Use the artifact-first recovery plan and small packet DAG:
docs/coordination/V17_RECOVERY_PLAN_ARTIFACT_FIRST.md
docs/coordination/V17_RECOVERY_PACKET_QUEUE.json
docs/coordination/V17_LIVE_CHECKPOINT_PROTOCOL.md
