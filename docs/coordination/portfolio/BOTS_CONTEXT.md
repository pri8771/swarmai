# Social Bots context carried into Codex

## Identity and scope

Repository `pri8771/astra-bot-launch`, project subdirectory `social-bots/`. This launcher contains other historical projects: do not edit them as part of Social Bots.
Canonical coordination `chatgpt/social-bots-plan-20260920`.
Current native integrator branch `fable/social-bots-v23-fasttrack-20260921`; the v23 name is historical. Its ref read here was 347de2b5c91c774670c3b884cc542c88fa6eabfa. The current note identifies that as a lead direction update, not a newly verified runtime release.

LEAD-048 / latest native owner direction: **LIVE V1.7, then stop development**. V1.8–V3 and future planning are parked. Read `social-bots/coordination/SESSION_START.md`, `state/CURRENT.md`, current assignment, `delivery/V17_LIVE.md`, `delivery/V17_SCOPE.json`, `delivery/V17_ACCEPTANCE.md` from the canonical ref. Paths after social-bots in this paragraph are relative to that project directory.

The current official state note says V0.4.x, with V0.3 accepted. Historical a204ad... and worker-reported 523 discovered/521 passed/2 skipped are not fresh independent live evidence. Do not equate a v23 branch name or that test count with V1.7.

## Product intent and independence

The goal is three distinct personas (social-a/social-b/social-c), durable scoped memory, real research, adaptive reasoning, safe account connections, measurement/learning and bounded operation. This must remain fully functional without SwarmAI. Portfolio coordination is not permission to import Swarm's runtime, data or credentials.

Current V1.7 community surface is real permitted read-only community/thread observation -> scoped memory -> observe/ignore/reply-draft decision -> correctly bound reviewed candidate -> outcome/next check. A reviewed draft is not a sent reply/DM. Do not fabricate comments, engagement, audiences or public outcomes.

## Exact session rule

One genuinely fresh session emits ONE `SESSION_ONCE` heartbeat after reading current direction. Resume/compaction emits no additional heartbeat; lease renewal/invocation completion is separate. No periodic chat watcher and no kept-open-chat proof. Do not diagnose the absence of five-minute ticks as a dead worker. Resolve the native assignment/heartbeat path rather than borrowing Swarm's path.

Fable is the named integrator; legacy Cursor lanes parked, QA review-only. Verify actual current owner before Codex writes code. Preserve incoming work/path ownership. A clean takeover does not reopen new lanes or erase old session history.

## Native task filter

V17_SCOPE active features are SB-R07 recovery, SB-V04..SB-V17, SB-S08..SB-S17 and necessary foundation/control/persona/account/canary/evidence work. Active delivery cards: C01, C04-C11, C19-C24. C02 is only schemas actually exchanged by the V1.7 runtime. C03/SB-S20-000 permits necessary consolidation of existing producers, not a V2 program. C25 is general-bot community work and already-required cultural review, not full V1.8 rollout. C26 is basic safe operation/stop/restart/recovery, not a V1.9/72-hour campaign.

C12-C18 and C27-C31 are parked, as are future specialists, strategy/decomposition, institutional-memory, segmentation/growth, multi-brand and portfolio-allocation programs. Preserve unused future code but keep it unreachable from the V1.7 dispatcher; repair only a shared unsafe boundary if needed. Do not make future acceptance a prerequisite to current delivery.

## Real evidence to preserve

V0.4 persona/evidence comparison needs fresh specific model authorization and frozen material-divergence criteria. V0.5 claims must link captured source bytes through support/reviewer events to the final rendered content hash; factual/voice/platform and applicable cultural gates remain. V0.6 must show all three real unpublished adaptive loops with prospective experiments and truthful learning.

V0.7 requires native scheduling on the verified persistent host, at least three distinct firings, contention, crash/restart/stale takeover, and two actual engineering-artifact -> lead -> later-worker cycles. A chat heartbeat is not that host scheduler.

V0.8 retains required X/Instagram/TikTok/Reddit/Facebook route/account/capability/analytics checks. An unavailable route is an explicit blocker. V0.9 public canaries require exact specific approval, destination/content readback and real windows. Uncertain results reconcile before retries. This setup grants no new public canary or model call.

V1.0 ordinary scheduled production loops must serve all three bots without starvation. V1.1 covers safe recovery. V1.2 route choice uses real availability; NO_PLATFORM is valid. V1.3 analytics preserve raw sources, units, observation windows and snapshot-vs-delta semantics; missing is not zero. V1.4 knowledge/hypotheses are persona-scoped and evidence-driven. V1.5 intervention closeout may be success/failure/inconclusive/stopped, not fabricated business success. V1.6 produces materially platform-native reviewed content with lineage/repetition control. V1.7 uses the real community path above and independent SB-V17-003 review.

Before a counted integrated run, freeze actual candidate/config/host, bot-account bindings, grants/budgets, schedule and numeric predicates. The native acceptance file proposes 24h continuity subject to lead freeze/replacement before execution; it is not a promised duration to completion. Do not import the parked 72h requirement or shorten actual platform observation windows.

Keep OFFLINE_FIXTURE, REAL_PROCESS, NATIVE_SCHEDULER, LIVE_SOURCE, LIVE_MODEL, LIVE_ACCOUNT, LIVE_PUBLIC_EFFECT and LIVE_ANALYTICS separate. Test refusal with harmless sentinels, not unauthorized real calls. Preserve failed experiments and payload/reviewer/grant binding.

## Accounts, finish and sources

Existing necessary owner-controlled account/verified Unsubscriber-alias setup permission is distinct from model/public/spend authorization. Read the actual grant and verify the mechanism. No secrets, public replies, engagement manipulation, payment/PAYG, main merge or extra live-model authority inferred here.

Finish as READY_FOR_LEAD_REVIEW, FAIL, INCONCLUSIVE or exact BLOCKED_AUTHORIZATION/HOST/ACCOUNT/DATA. Only the native lead accepts. Development stops at accepted live V1.7; existing service operation must still remain within its own grant.

Sources read: canonical CLAUDE.md (2041052...), SESSION_START (9db58c...), CURRENT (619ce65...), V17_SCOPE (567ef79...), V17_ACCEPTANCE (9fb6864...), and actual integrator ref 347de2b.... No Bots runtime, provider/account or scheduler proof was performed for this hub.
