# R27e — direct Codex crash-window repair

State: **READY_FOR_LEAD_REVIEW**; recommendation **REVIEW_BLOCKED** for independent formal verdict. Codex authored and tested this candidate; no self-acceptance.

- Native artifact ART-V17-APPROVAL-BINDING / R27e, existing SP3 transaction packet.
- Actual lead release: canonical `51270d612030beb1b43b87630103096d6b5688cf`, `assignments/CODEX_R27E_20260922.md`, accepted R27c-R1 review. Codex ACKed and implemented in its own worktree; no Fable handoff.
- Exact expected base `fb34b2c00fc7cc2c81cb481dfa35df8e259bc04e`; candidate `codex/swarm-r27e-recovery-20260922@193e454e0226f7e7be768ad810eb68e6050aea8f`. Original worker checkout/service and failed attempt2 remain unchanged.
- Source: `src/swarm/tools/effects.py`, `src/swarm/tools/v17_gateway.py`; new real-process fixtures/tests `tests/integration/db/{_effect_crash_child,test_effect_crash_window}.py`; two existing direct-repository tests now supply the required admission token. No schema migration.

## Behavior

Lazy recovery atomically changes expired executing effects to unknown/executor_lost; never reserved. The production grace default remains30seconds; tests alone use constructor grace0 with an actual1.1second elapsed wait after a real spawned child exits9. Stored timeout/class prevent a caller shortening a prior effect's recovery window or downgrading irreversible classification. Legacy executing rows lacking stored timeout fail closed for operator disposition.

Reconciliation records succeeded, proved-not-applied or pending outcomes, including malformed/exception probe results. Consequential proved-not-applied may retry under existing approval checks; irreversible remains durably blocked across later requests until explicit rearm records operator/reason/time. Revoked approval stays revoked.

Pre-observation and finalization require the admitted executor/attempt/state token at both gateway and repository boundaries. Missing tokens fail closed, so delayed old executors cannot overwrite recovered state, a newer attempt or observations. Concurrent reconciliation has one authoritative terminal disposition. This guard is necessary for the new takeover path, not a later actor-policy framework.

## Exact verification

**20 real-PostgreSQL crash-window/adverse tests passed five consecutive runs**; each run includes real process termination before/after a fsynced temporary-file effect. Post-effect crash reconciles once without re-execution; pre-effect crash retries exactly once with ordered failed/succeeded receipts and single grant consumption. Additional33admission/transaction/reservation tests pass. Full offline suite **494passed,0skipped**; Ruff/mypy169 pass. [Commands/exits/tree](evidence/CODEX-R27E-20260922/checks.json), [process-kill output/full checks](evidence/CODEX-R27E-20260922/checks.txt), [hashed manifest](evidence/CODEX-R27E-20260922/manifest.json).

The isolated Swarm database and synthetic loopback/console fixtures provide engineering evidence. They do not satisfy CP5, CP6, genuine provider/host/time or other live gates. R28a's deadline enforcement remains a separate held packet; this recovery slice relies on timeout+grace and does not claim its unimplemented deadline guarantee. No provider/model call, real external issue action, main merge, deployment, paid fallback, new scheduler or CP1 attempt occurred.

Requested action: ChatGPT independently reviews exact candidate and records verdict; release R28a to Codex only if the native hold is satisfied. Codex owns repair findings directly.

## Hosted CI readback

Exact193e454 Actions runs35685737614 (pull_request) and35685735450 (push) each show offline/console/live-gated FAILURE with zero executable steps. Check annotations report account payment/spending-limit restriction. This is not passing hosted CI or an observed code-test failure. Local exact-source checks remain the verification evidence; no billing change, spend increase or runner workaround was attempted.
