R28b-1 pre-edit mechanical diagnosis
Baseline: b204fda738221787325ce8fd0b8db58e6e29b573
Canonical assignment/packet read at 11d9d8f; scope rules 1-3 only.

Synthetic red reproduction
- Script: /tmp/swarm-r28b1-red-repro.py
- Output: /tmp/swarm-r28b1-red-repro-output.json
- Command: PYTHONPATH=src ../swarm-source/.venv/bin/python /tmp/swarm-r28b1-red-repro.py
- Result: exit 0. No provider/network/database/live action; approvals used only in a
  process-local synthetic store for the two cases that otherwise require approval.
- Underclassification: ApiMcp manifest says consequential/medium; envelope changed to
  none/low. It succeeded, called adapter once, used no approval, and receipt has no
  effective classification fields.
- Missing authority: consequential envelope with mission/task/attempt all None succeeded
  and called adapter once because normalize synthesized generations 1/0.
- Undeclared operation: `undeclared.synthetic` succeeded and called adapter once.

Evidence correction / real repository reproduction
- The first synthetic run used a fake durable flag for missing-authority and undeclared
  cases. Those two observations are superseded as durable-boundary evidence (the
  underclassification Memory-store case remains valid for its bypass behavior).
- Re-ran missing-authority and undeclared-operation with actual DurableEffectRepository
  in dedicated database swarm_r28b1_agent_36370 on the owned temporary cluster Unix
  socket /tmp/swarm-pgcheck.QxqRvZ, port 56421.
- Script: /tmp/swarm-r28b1-real-pg-repro.py
- Output: /tmp/swarm-r28b1-real-pg-repro-output.json
- Both cases again succeeded, called adapter once, and persisted state=succeeded. Missing
  authority remained mission/task/attempt=None with synthesized generations 1/0.
- Dropped only the dedicated database and verified pg_database no longer contains it.
- No provider/network/live action or production grant was used.

Causal paths
- actions.py:44-49 gives caller-controlled class/risk and valid-looking generation
  defaults. ActionReceiptV17 (actions.py:114 onward) has no effective-class fields.
- AdapterManifest has only global class/risk/scopes and no operations mapping.
- all three normalizers synthesize `int(request.get(..., 1/0))`:
  api_mcp.py:64-65, local_sandbox.py:48-49, browser_session.py:59-60.
- gateway execute_envelope uses envelope class/risk directly in durable and approval
  decisions; _authorize_project checks scopes/manifest but never operation membership.
- _check_generations only compares synthesized integers with constructor integers.
- the durable admission reader is installed only when any mission/task/attempt is present;
  all absent is treated as a valid standalone action, so repository completeness checks
  never run.

Minimal regressions
1. Declared consequential operation + envelope none/low + complete synthetic authority +
   real DurableEffectRepository, no approval => approval_required and adapter0.
2. Operation absent from manifest => operation_not_declared before pre-observe/execute;
   assert adapter0 and no effect row.
3. Parametrize missing mission/task/attempt/lease_generation/cancellation_generation for
   declared consequential and irreversible operations => fence_missing before approval or
   adapter action.
4. Successful leased consequential execution => receipt effective class/risk equal the
   higher manifest declaration; additionally test a higher envelope claim wins.
5. Normalizer omission asserts both generation fields are None, never synthesized 1/0.

Adapter manifest compatibility
- ApiMcpAdapter: declare `echo` consequential/medium, scopes network.https+mcp.call,
  remote_resource read/write.
- LocalSandboxAdapter: declare `write_text` idempotent/low, scope sandbox.fs,
  workspace_file read/write.
- BrowserSessionAdapter: declare `navigate` none/low with browser.session and page_dom
  read/no form write; `submit` consequential/high with browser.session, page_dom read and
  form_submit write. Existing unknown-operation adapter denial becomes unreachable through
  the gateway because undeclared operation must be rejected first; direct adapter behavior
  can remain.
- Normalizers should pass missing generations as None (avoid int(None)); requested scopes
  should remain compatible with declared operation scopes.

Fixture/test maintenance without weakening classification
- Reuse/extend test_effect_admission_fences.py::_leased_envelope for every successful real
  PostgreSQL consequential/irreversible path. Make it accept request overrides/body so
  crash adapters retain their file payload while receiving real mission/task/attempt and
  generations. Keep its authoritative mission/task/worker/attempt creation unchanged.
- test_effect_crash_window.py and its child: setup_effect must use a leased envelope; keep
  real kill, outcome, recovery, attempt, approval, and race assertions unchanged. Expand
  cleanup to the lease/mission tables or reuse the admission-fence factory.
- test_effect_transactions.py, test_action_receipts_durable.py,
  test_effect_reservation_atomic.py, and test_approval_integrity.py: successful adapter
  paths require leased envelopes. Approval negatives that fail before begin may use complete
  non-None synthetic authority, but success/race/retry/crash paths must use actual rows.
- tests/tools/test_v17_gateway_negatives.py: local write/navigate remain standalone by their
  declarations. API success/dedupe and browser submit success use leased PG envelopes.
- tests/tools/test_v17_gateway_failclosed.py: ResultAdapter must declare operation `echo`
  explicitly. Memory parameter declares echo none/low; PostgreSQL parameter declares echo
  consequential with real leased authority. Do not mutate only the old global manifest
  class, advertise fake durability, or downgrade the PG case.
- RaisingAdapter remains a declared consequential echo operation and preserves the proven
  AdapterNotSentError seam; its retries/approval/race coverage should use leased authority.

R28b-2 remains held: no FenceProvider, PolicyProvider, ActorContext, provider DB reader,
constructor seam replacement, or scope-intersection implementation belongs in this slice.

Reusable helper candidate
- /tmp/swarm-r28b1-leased-fixture-helper.py creates actual mission/task/worker/claimed
  attempt authority and supplies generation values read from claim.worker_generation and
  mission.cancellation_generation. It rejects caller overrides of authority fields and
  accepts payload/operation/body overrides for crash and adapter suites. Ruff and
  py_compile pass; root should integrate it into a repository test helper location.
