# R28d-1 local sandbox operations — engineering review preparation

Recommendation: **RECOMMEND_ACCEPT — bounded R28d-1 engineering only.** ChatGPT remains formal acceptance authority; R28d-2 remains held.

- Released base: `1ea1ca55a2470d94fe704d64c029a991c3c0dea5`, tree `1b8577c91b2984b804ea6df259e6a47a697ccc31`.
- Candidate: `a2cfb3c1d5e326a1b15d7d23cfe3c91ea2e40bf4`, tree `d4a76d4f697194fc649b16edc00fdeb3eeb1ad9b`.
- Native release: `coordination/swarm-control@bd08b419f2d4d61913a85b3921474e1aef942da7`, assignment `CODEX_R28D1_20260922.md`.

## Causal repair

The accepted adapter had only `write_text`, optional root configuration, no process operation, and insufficient confinement: an owned temporary-directory probe followed a root symlink and wrote outside the injected root. The probe and cleanup are retained.

The candidate replaces that surface with `fs.write_text` and `proc.run`, both idempotent/low. File writes require a relative path without `..`; resolved paths must remain under the injected root, including symlink resolution. Rejections use `AdapterDeniedError("path_outside_root")`. Pre-observation is a SHA-256 or null and post-observation records SHA-256 plus byte count.

`proc.run` accepts only argv arrays whose prefix matches the released fixed allowlist, always uses the injected root as cwd, and invokes `subprocess.run` with `shell=False`. The outcome records a nonzero process exit as a completed adapter operation, full stdout/stderr hashes, and trailing output capped at 4,096 bytes. Disallowed commands are rejected before subprocess invocation. Runtime root and allowlist remain instance configuration rather than new manifest/trust lifecycle data.

## Evidence

The new seven-test focused module passed. The combined focused R27/R28/R29 compatibility selection passed 42 with 14 expected environment skips. A first compatibility run preserved two caller failures from the removed `write_text` operation and old scope/error expectations; those callers were migrated explicitly, not restored by an alias. Durable PostgreSQL compatibility passed 46 tests with cleanup zero. Full offline pytest passed 411 with 208 database-dependent skips. Full PostgreSQL pytest passed **606** with 13 existing environment skips in 26.78 seconds; cleanup zero. Ruff passed and mypy passed all 173 source files.

The runs use only owned local temporary directories and a disposable PostgreSQL database. No brokered local mission, RepoWorker/MissionRuntime wiring, remote host, provider/model/network/public action, CP1 attempt, spend, deployment, main merge or scheduler modification occurred.

## Requested verdict

Please accept or reject the exact R28d-1 candidate. If accepted, release only the next bounded R28d-2 packet. The R730 is owner-authorized for a later host lane, but it is not evidence here and cannot satisfy R18 until its protocol/dependency release is accepted.

Evidence: [CODEX-R28D1-20260922](../evidence/CODEX-R28D1-20260922/).
