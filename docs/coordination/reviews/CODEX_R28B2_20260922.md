# R28b-2 — authenticated actor, policy and durable fence authority

State **READY_FOR_LEAD_REVIEW**; recommendation **RECOMMEND_ACCEPT** for released R28b rules 4–6 only. Codex implemented and prepared this evidence; ChatGPT engineering lead remains the independent formal acceptance authority.

- Repository `pri8771/swarmai`; branch `codex/swarm-r28b2-authority-20260922`; exact source **a06b8a82925daa4dd08a137cb1eb724aaa2d4efb**, tree **20a1c9b5ea240c009d41c5a5effb1c075aa78a70**; accepted parent **2ebaa5a0cd64b2de665202c5cf9db0dd4f0a1510**. [Draft PR23](https://github.com/pri8771/swarmai/pull/23). Native assignment/packet released at **4fa4bd066c2a6415b4f1f9da84ef895d3882adb7**; latest fetched canonical observed **933fe98** contains later checkpoint acknowledgment/heartbeat updates, not broader implementation authority.
- Red-before: on the accepted parent, stale policy, an actor lacking the requested `admin.write` authority, and envelope generations matching constructor constants while modeled durable generations had advanced all reached the reservation boundary. The retained harness stops at `reserve()`: zero adapter calls, no reservation mutation, no database/provider/network action. A previously accepted forged-actor diagnostic separately established that caller-controlled actor reached reservation.
- Causal repair: new `ActorContext`, `PolicyProvider`, `FenceProvider`, static bounded providers and durable `LeaseFenceProvider`; gateway constructor authority constants were removed. Execution/reconciliation now reject context project, actor, stale policy, declaration-scopes union requested-scopes, and stale durable generations in native order. Context propagates through request normalization, reconciliation and the not-applied retry; session recovery requires and forwards authenticated context.
- Durable authority remains DB-owned. `read_current_fence` reads the exact project/mission/task/attempt-bound TaskAttempt generation and Mission cancellation generation under `FOR SHARE`. The provider precheck uses that narrow read. The committed reader calls the pre-existing `LeaseLifecycleService.read_effect_fence` directly, preserving mission → lease → attempt lock order plus active lease, expiry, worker revocation and full binding checks. A cancellation committed between precheck and admission leaves the approval unused and effect reserved with zero adapter calls.
- Approval helpers remain scoped after constructor-project removal: make checks project+actor, put checks project, and revoke derives `revoked_by` from authenticated context. `FenceState.authority` is reserved but unread by V1.7. No R28c registry work is included.
- Review corrections are preserved rather than erased. The initial authority test red used a different repository instance than the gateway for its cancellation hook and omitted approvals in two later-fence cases; no cancellation occurred, so the snapshot hypothesis was withdrawn. Corrected tests bind the actual gateway store and assert hook execution. An independent mechanical review found policy/scope were ordered after missing-fence validation; production order was corrected and four conflicting-failure regressions prove policy/scope denial before fence provider/store/adapter. Initial mypy dict-expansion failure was also corrected. [Provenance](evidence/CODEX-R28B2-20260922/test-provenance.txt).
- Verification: **578 passed in 43.76s**, Ruff passed, mypy passed for **171 source files**. Focused retained results: **19 authority**, **66 gateway compatibility**, and **71 affected PostgreSQL compatibility**, with agent-owned unique database cleanup count `0`. [Commands](evidence/CODEX-R28B2-20260922/commands.txt), [checks](evidence/CODEX-R28B2-20260922/checks.json), [causal map](evidence/CODEX-R28B2-20260922/causal-map.md), [mechanical review](evidence/CODEX-R28B2-20260922/mechanical-review.txt), [hash manifest](evidence/CODEX-R28B2-20260922/manifest.json).
- Limits: engineering evidence only. No genuine live checkpoint, provider/model call, external effect, credential use, spend, deployment, main merge or scheduler change. Mechanical agent review is not formal acceptance. **R28c remains HELD** pending the requested independent exact-SHA verdict.
- Requested verdict: inspect exact PR23/source/evidence and either accept R28b-2 or return bounded rework. Only after acceptance may the lead release the next dependency-safe native packet.

## Hosted CI readback

At 2026-09-22T06:54Z, exact candidate a06b8a8 had two CI runs (35695802892 and35695770245), six jobs, zero acquired runners and zero executed steps. GitHub explicitly reports an account payment/spending-limit gate. This is not a code test failure or a passing CI result; no billing changes or reruns were made. [Exact run/job readback](evidence/CODEX-R28B2-20260922/hosted-ci.json). Local engineering results above remain the executed verification.
