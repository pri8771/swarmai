# R28a — diagnosed gateway outcome failures

State: IN_PROGRESS / direct Codex ACK, not READY or accepted. Assignment and native packet read at canonical8695019ffee77719ddd2fcc80affd7e42fe95bdd; prerequisite R27e independently accepted at exacte924351bd93510eaae279225db3a2fa447dd6894. Actual branch codex/swarm-r28a-outcomes-20260922 in a new isolated worktree at that base. No Fable handoff, original worker service change or live action.

Owner has expanded Swarm goal to V2.0 and requested systematic debugging before code/tests. See ../CODEX_GOAL_20260922.md. This current SP2 remains R28a only.

## Reproduction and cause before repair

Synthetic black-box script at exacte924351 produced:
- empty result -> succeeded;
- unrecognized outcome -> succeeded;
- arbitrary transport exception -> failed;
- deadline1s with1.2s adapter -> succeeded at1.201s;
- post-observation exception -> raised RuntimeError, row remained executing;
- omitted store -> accepted with InMemoryEffectStore;
- unobservable echo reconciliation -> failed/no_prior_success.

[Observed output](evidence/CODEX-R28A-20260922/baseline-observed.txt), [reproducer](evidence/CODEX-R28A-20260922/baseline-reproduce.py), [bounded agent causal map](evidence/CODEX-R28A-20260922/causal-map.txt). No real destination/DB/provider/model used. These are debug reproductions, not live proof.

Root causes: success is the default/fallthrough of _outcome_from_result and the execute branch; generic exceptions are mapped to certain failure; synchronous adapter calls block delivery of timeout/cancellation; observation errors occur outside outcome protection; constructor silently supplies memory; echo transport cannot observe destination application.

## Repair underway

Exact native outcome mapping, explicit adapter errors, required effect store, durable-store prerequisite for consequential/irreversible effects, bounded thread calls, cancellation receipt plus rethrow, post-observation error isolation and conservative echo reconciliation. LocalSandbox and Browser unknown-operation return vocabulary must be made compatible with the explicit adapter contract; these narrow adapter edits are necessary to preserve existing behavior. No automatic retry/backoff or extra scheduler.

Verification will start with these reproduced paths, then required negative cases and affected real-PG checks. The pending candidate is not accepted and no passing result is claimed yet. Jobs/Bots diagnostics remain in their native repositories; no shared queue/runtime is introduced.
