# SwarmAI V1.9 Status — Extensions / install implementation-complete (local)

Extension registry/loader (gateway-only), install/upgrade/rollback plans,
redacted support bundles, and self-merge ban are implemented.
CLI: `swarm install *`, `swarm extensions demo-scopes`.
Fresh/external install evidence: **pending**.
